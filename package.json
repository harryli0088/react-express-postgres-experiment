{
  "name": "express-postgres-experiment",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && nodemon index.js",
    "start": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^4.0.1",
    "nodemon": "^1.19.4"
  }
}
